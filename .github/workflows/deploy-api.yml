name: Deploy API

on:
  push:
    branches: [ "main" ]
    paths:
      - 'apps/api/**'
      - 'packages/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Placeholder: Add deployment steps for Vercel, Railway, Render, etc.
      - name: Deploy Placeholder
        run: echo "API deployment not configured yet"
