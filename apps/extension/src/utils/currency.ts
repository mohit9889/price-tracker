/**
 * Thin wrapper around the shared formatPrice utility.
 * Import from here instead of duplicating the Intl.NumberFormat logic locally.
 */
export { formatPrice } from '@price-tracker/shared-utils';
